import React from "react";
import ReactDOM from "react-dom/client";
import ChannelOfDignity from "./ChannelOfDignity";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ChannelOfDignity />
  </React.StrictMode>
);
